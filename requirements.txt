# Landmark-based Drowsiness Detection System Requirements with GPU Acceleration
# =============================================================================

# Core dependencies
flask>=2.3.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
requests>=2.31.0

# Computer vision and machine learning
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.11.0

# GPU acceleration dependencies (optional - will fallback to CPU if not available)
# CuPy for GPU-accelerated NumPy operations (CUDA 12.x compatible)
cupy-cuda12x>=12.0.0; platform_machine=="x86_64"

# Alternative requirements for different CUDA versions (uncomment as needed):
# cupy-cuda11x>=11.0.0; platform_machine=="x86_64"  # For CUDA 11.x
# cupy-cuda10x>=10.0.0; platform_machine=="x86_64"  # For CUDA 10.x

# Note:
# - OpenCV with CUDA support requires opencv-contrib-python compiled with CUDA
# - For production deployment, consider using opencv-contrib-python-headless
# - CuPy installation requires compatible CUDA toolkit (12.x for cupy-cuda12x)
# - All other dependencies (sqlite3, concurrent.futures, threading, etc.)
#   are built-in Python modules and don't need to be installed via pip

# Development and testing dependencies (optional)
# pytest>=7.0.0
# pytest-cov>=4.0.0
