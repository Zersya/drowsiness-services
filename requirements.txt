# Landmark-based Drowsiness Detection System Requirements
# =====================================================

# Core dependencies
flask>=2.3.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
requests>=2.31.0

# Computer vision and machine learning
opencv-python>=4.8.0
numpy>=1.24.0
dlib>=19.24.0
scipy>=1.11.0

# Note: All other dependencies (sqlite3, concurrent.futures, threading, etc.)
# are built-in Python modules and don't need to be installed via pip
